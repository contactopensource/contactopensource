plan_gist:
  title: PlanGIST
  summary: |-
    Plan GIST criteria: Goal, Idea, Step, Task.
    
  module: Plan
  attribute_groups:
    - !include ../_meta.yml
    - !include ../_card.yml
    - Main:
        - plan_id:
          help: "Plan"
          type: plan.id
          asscociation: belongs_to: plan
      - goal_card_id:
          help: "Goal"
          type: card.id
      - idea_card_id:
          help: "Idea"
          type: card.id
      - step_card_id:
          help: "Step"
          type: card.id
      - task_card_id:
          help: "Task"
          type: card.id
