plan_gist:
  summary: Plan with GIST criteria.
  module: Plan
  attribute_groups:
    - !include ../_meta.yml
    - !include ../_card.yml
    - Main:
      - goal_card_id:
          help: "Goal"
          type: card.id
      - idea_card_id:
          help: "Idea"
          type: card.id
      - step_card_id:
          help: "Step"
          type: card.id
      - task_card_id:
          help: "Task"
          type: card.id
