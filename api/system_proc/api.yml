system_proc:
  title: System Proc
  model: SystemProc
  summary: |-

    System process, such as for a running program.

  description: |-

    System process, such as for a running program.

  module: System
  attributes:
    pid:
      type: integer
      index: true
      example: 1000
    description:
      type: text
      index: true
      example: "/bin/sh --version"
