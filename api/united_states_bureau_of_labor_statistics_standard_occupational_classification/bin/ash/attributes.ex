
defmodule MyApp.MyDomain.UnitedStatesBureauOfLaborStatisticsStandardOccupationalClassification2018.Attributes do
  use Spark.Dsl.Fragment, of: Ash.Resource

  attributes do

    attribute :group, :string do
    end

    attribute :code, :string do
    end

    attribute :title, :string do
    end

    attribute :definition, :text do
    end

  end
end
