net_promoter_score_worker_perspective:
  title: Net Promoter Score (NPS) worker perspective
  model: NetPromoterScoreWorkerPerspective
  summary: |-

    Net Promoter Score (NPS) work perspective


  description: |-

    Net Promoter Score (NPS) worker perspective

  attribute_groups:
    - !include ../_meta.yml
    - Main:
      - actual:
          help: Actual value
          type: numeric(7,2)
      - expect:
          help: Target value
          type: numeric(7,2)
      - ratio:
          help: Ratio = (actual / expect)
          type: numeric(7,2)
          sql: ratio DECIMAL GENERATED ALWAYS AS (actual / expect) STORED
      - unit:
          help: Unit
          type: text
      - description_as_markdown:
          help: Description as markdown
          type: text
