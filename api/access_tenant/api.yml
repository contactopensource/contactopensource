access_tenant:
  title: Access Tenant
  model: AccessTenant
  summary: |-

    Access control tenant.

  description: |-

    Access control tenant.

  module: Access
  attributes:
    - !include ../_meta.yml
    - !include ../_card.yml
    - Main:
      - key:
          type: string
          index: true
          example: access_tenant_example
