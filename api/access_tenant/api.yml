access_tenant:
  summary: Access control tenant.
  module: Access
  model: Tenant
  table: access_tenants
  attributes:
    - meta
    - card
    - main:
      - key:
          type: string
          index: true
          example: access_tenant_example
