access_permission:
  title: Access Permission
  model: AccessPermission
  summary: |-

    Access control permission, which joins access_attribute and access_operation.

  description: |-

    Access control permission, which joins access_attribute and access_operation.

  module: Access
  title: AccessPermission
  model: AccessPermission
  attribute_groups:
    - join:
      - access_attribute_id:
          type: access_attribute.id
          example: access attribute "reader"
          belongs_to_model: AccessAttribute
      - access_operation_id:
          type: access_operation.id
          example: access operation "read"
          belongs_to_model: AccessOperation
