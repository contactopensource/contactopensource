access_agent:
  summary: Access control agent.
  module: Access
  model: Agent
  table: access_agents
  attribute_groups:
    - meta
    - card
    - main:
      - key:
          type: string
          index: true
          example: access_agent_example
  # uml: access_agent ||-r-o{ access_agent_locale
