burn_rate_net_cash_per_week:
  title:  Burn rate net cash per week
  model: BurnRateNetCashPerWeek
  summary: |-

    Burn rate net cash per week

  description: |-

    Burn rate net cash per week: actual, target, ratio, unit, description.

  attribute_groups:
    - !include ../_meta.yml
    - Main:
        - actual:
            help: Burn rate net cash per week actual value
            type: numeric(7,2)
        - target:
            help: Burn rate net cash per week target value
            type: numeric(7,2)
        - ratio:
            help: Burn rate net cash per week ratio = (actual / target)
            type: numeric(7,2)
            sql: ratio DECIMAL GENERATED ALWAYS AS (actual / target) STORED
        - unit:
            help: Burn rate net cash per week unit
            type: string
        - description:
            help: Burn rate net cash per week description
            type: string
