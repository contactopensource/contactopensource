system_user:
  title: System User
  model: SystemUser
  summary: |-

    System user, such as for managing system file access.

  description: |-

    System user, such as for managing system file access.

  module: System
  attributes:
    uid:
      summary: uid
      type: integer
      index: true
      example: 1
    key:
      summary: key
      type: string
      index: true
      example: root
