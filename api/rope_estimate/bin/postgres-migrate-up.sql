CREATE TABLE rope_estimate (
);

CREATE INDEX index_rope_estimate_id on rope_estimate(id);
CREATE INDEX index_rope_estimate_zid on rope_estimate(zid);
CREATE INDEX index_rope_estimate_title on rope_estimate(title);
CREATE INDEX index_rope_estimate_subtitle on rope_estimate(subtitle);

