access_assignment:
  summary: Access control assignment, which joins an access_agent and an access_attribute.
  attributes:
    access_agent_id:
      type: access_agent(id)
      example: access agent 'alice'
    access_attribute_id:
      type: access_attribute(id)
      example: access attribute 'reader'
