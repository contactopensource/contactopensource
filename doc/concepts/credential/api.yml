credential:
  summary: A credential, such as an award, badge, certification, diploma, endorsement, etc.
  module: TODO
  attributes:
    key:
      type: string
      index: true
      example: credential_example
credential_locale:
  summary: locale
  module: TODO
  attributes:
    credential_id:
      type: credential(id)
      example: credential id for "United States Passport".
    locale_id:
      type: locale(id)
    name:
      type: string
      index: true
      example: United States Passport
    summary:
      type: string
      example: United States Passport Government Document
    description:
      type: text
      example: United States Passport Government Document suitable for international travel
    disambiguation:
      type: text
      example: This text explains the difference between this item and others.
