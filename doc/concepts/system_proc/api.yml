system_proc:
  summary: System process, such as for a running program.
  module: System
  attributes:
    pid:
      summary: process id
      type: integer
      index: true
      example: 1000
    key:
      summary: description of running process
      type: text
      index: true
      example: '/bin/sh --version'
