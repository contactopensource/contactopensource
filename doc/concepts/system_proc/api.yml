system_proc:
  summary: System process, such as for a running program.
  attributes:
    pid:
      summary: pid
      type: integer
      index: true
      example: 1000
    key:
      summary: name
      type: text
      index: true
      example: /bin/sh --version
