    # def entity_attribute_index_id(entity, attribute):
    #     if entity is None or attribute is None:
    #         return ''
    #     return f"index_{entity.id}_{attribute.id}"

    # attribute_type_map = {
    #     'latitude', 'latitude',
    #     'longitude', 'longitude',
    #     'url', 'text',
    # };

